export default `{{dependencyHeader}}{{dependencyCode}}
{{siblingHeader}}{{siblingDeclarations}}
Generate {{targetLanguage}} code for the function below.

Implement the logic described in the comments and annotations:

{{declaration}}
`
